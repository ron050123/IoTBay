@model IEnumerable<IoTBay.web.Models.Entities.Order>

<!DOCTYPE html>
<html>
<head>
    <title>Ordered Products</title>
    <style>
        .order-container {
            margin: 20px;
        }
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="order-container">
        <h1>Ordered Products</h1>
        <table>
            <thead>
                <tr>
                    <th>Order ID</th>
                    <th>User ID</th>
                    <th>Product ID</th>
                    <th>Product Name</th>
                    <th>Quantity</th>
                    <th>Order Date</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Order in Model)
                {
                    <tr>
                        <td>@Order.OrderId</td>
                        <td>@Order.UserId</td>
                        <td>@Order.ProductId</td>
                        <td>@Order.ProductName</td>
                        <td>@Order.Quantity</td>
                        <td>@Order.OrderDate</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>